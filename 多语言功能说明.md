# 🌍 多语言功能说明

你的网站现在已经支持中英文双语切换功能！

## ✨ 功能特点

- 🇨🇳 中文（默认语言）
- 🇺🇸 英文
- 🔄 一键切换语言
- 🌐 自动保存用户语言偏好
- 📱 所有页面完全翻译
- 🔗 SEO 友好的多语言 URL

---

## 🚀 如何使用

### 访问网站

**默认语言（中文）**：
- http://localhost:3000
- http://localhost:3000/shop
- http://localhost:3000/about

**英文版本**：
- http://localhost:3000/en
- http://localhost:3000/en/shop
- http://localhost:3000/en/about

### 切换语言

1. **通过导航栏切换**
   - 点击顶部导航栏的 🌐 地球图标
   - 在下拉菜单中选择语言：
     - 🇨🇳 中文
     - 🇺🇸 English

2. **通过URL切换**
   - 在 URL 前添加 `/en` 切换到英文
   - 移除 `/en` 切换回中文

---

## 📁 项目结构变化

### 新增文件

\`\`\`
reiboreal-website/
├── messages/                    # 翻译文件
│   ├── zh.json                 # 中文翻译
│   └── en.json                 # 英文翻译
│
├── i18n/                       # 国际化配置
│   ├── config.ts               # 语言配置
│   ├── request.ts              # 请求配置
│   └── routing.ts              # 路由配置
│
├── middleware.ts               # 语言路由中间件
│
├── components/
│   └── LanguageSwitcher.tsx   # 语言切换器
│
└── app/
    └── [locale]/               # 多语言路由
        ├── layout.tsx         # 布局（支持多语言）
        └── page.tsx           # 首页（支持多语言）
\`\`\`

### 重要说明

⚠️ **现在所有页面都必须在 `app/[locale]/` 目录下创建**

例如：
- 旧路径：`app/shop/page.tsx`
- 新路径：`app/[locale]/shop/page.tsx`

---

## 🛠️ 如何添加新翻译

### 1. 更新翻译文件

在 `messages/zh.json` 添加中文：
\`\`\`json
{
  "mySection": {
    "title": "我的标题",
    "description": "我的描述"
  }
}
\`\`\`

在 `messages/en.json` 添加英文：
\`\`\`json
{
  "mySection": {
    "title": "My Title",
    "description": "My Description"
  }
}
\`\`\`

### 2. 在组件中使用翻译

\`\`\`tsx
'use client'

import { useTranslations } from 'next-intl'

export default function MyComponent() {
  const t = useTranslations('mySection')

  return (
    <div>
      <h1>{t('title')}</h1>
      <p>{t('description')}</p>
    </div>
  )
}
\`\`\`

---

## 📝 已翻译的内容

### 导航栏
- 首页 / Home
- 产品系列 / Shop
- 品牌故事 / Stories
- 关于我们 / About

### 首页
- 标题和副标题
- 品牌理念
- 精选系列
- Newsletter 订阅区

### 页脚
- 快速链接
- 客户服务
- 版权信息

### 其他
- 购物车
- 产品详情
- 博客
- 关于页面

---

## 🌐 URL 规则

### 默认语言（中文）
- 不显示语言前缀
- `/` → 首页
- `/shop` → 产品页

### 其他语言（英文）
- 显示语言前缀
- `/en` → 首页
- `/en/shop` → 产品页

### 自动跳转
- 用户访问 `/` 时：
  - 如果浏览器语言是英文 → 自动跳转到 `/en`
  - 否则 → 保持在 `/`

---

## 🎯 语言切换原理

1. **用户点击语言切换按钮**
2. **系统更新 URL** (例如：`/shop` → `/en/shop`)
3. **保存语言偏好到 Cookie**
4. **页面重新加载对应语言的内容**
5. **下次访问自动使用保存的语言**

---

## 💡 高级用法

### 1. 带参数的翻译

在 `messages/zh.json` 中：
\`\`\`json
{
  "greeting": "你好，{name}！"
}
\`\`\`

使用：
\`\`\`tsx
{t('greeting', { name: '张三' })}
// 输出：你好，张三！
\`\`\`

### 2. 复数形式

\`\`\`json
{
  "items": "{count, plural, =0 {没有商品} =1 {1 件商品} other {# 件商品}}"
}
\`\`\`

### 3. 日期和数字格式

\`\`\`tsx
import { useFormatter } from 'next-intl'

const format = useFormatter()

// 格式化日期
format.dateTime(new Date(), { dateStyle: 'long' })

// 格式化数字
format.number(1234.56, { style: 'currency', currency: 'CNY' })
\`\`\`

---

## 📦 迁移现有页面

如果你想让现有的其他页面支持多语言，需要：

### 步骤 1: 移动文件

将页面从 `app/xxx/page.tsx` 移动到 `app/[locale]/xxx/page.tsx`

### 步骤 2: 添加翻译

在 `messages/zh.json` 和 `messages/en.json` 中添加翻译内容

### 步骤 3: 使用翻译

更新组件使用 `useTranslations` hook：

\`\`\`tsx
import { useTranslations } from 'next-intl'

export default function Page() {
  const t = useTranslations('pageName')

  return <h1>{t('title')}</h1>
}
\`\`\`

### 步骤 4: 更新链接

将 `Link` 从 `next/link` 改为 `@/i18n/routing`：

\`\`\`tsx
// ❌ 旧的
import Link from 'next/link'

// ✅ 新的
import { Link } from '@/i18n/routing'
\`\`\`

---

## 🐛 常见问题

### Q: 切换语言后页面 404？

A: 确保页面文件在 `app/[locale]/` 目录下

### Q: 翻译不显示？

A: 检查：
1. 翻译 key 是否拼写正确
2. JSON 文件格式是否正确
3. 是否重启了开发服务器

### Q: 如何添加第三种语言？

1. 在 `i18n/config.ts` 中添加语言代码
2. 创建 `messages/xx.json` 文件
3. 重启服务器

---

## 🎨 自定义语言切换器

语言切换器组件位于 `components/LanguageSwitcher.tsx`

你可以自定义：
- 样式
- 图标
- 显示方式（下拉菜单 / 内联按钮）
- 动画效果

---

## 📊 SEO 优化

多语言网站自动包含：
- `<html lang="zh">` 或 `<html lang="en">`
- 正确的 URL 结构
- 语言切换链接

未来可以添加：
- `<link rel="alternate" hreflang="en" href="/en" />`
- 多语言 sitemap
- 结构化数据

---

## ✅ 测试清单

- [ ] 访问中文版首页（/）
- [ ] 访问英文版首页（/en）
- [ ] 点击导航栏的语言切换器
- [ ] 切换到英文，检查所有文字是否翻译
- [ ] 切换回中文
- [ ] 在不同页面测试语言切换
- [ ] 刷新页面，语言保持不变
- [ ] 检查移动端语言切换

---

## 🚀 下一步

你现在可以：
1. **继续完善翻译**：添加更多页面的翻译
2. **测试功能**：在浏览器中测试语言切换
3. **自定义样式**：调整语言切换器的外观
4. **添加更多语言**：支持更多语言（如日语、韩语等）

---

**恭喜！你的网站现在是国际化的了！** 🌍✨

有任何问题随时问我！
